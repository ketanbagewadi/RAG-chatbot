# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # for file uploads

# ── Vector DB ──────────────────────────────────────────────────────────────────
chromadb==0.5.0

# ── LangChain (document loaders + splitter) ────────────────────────────────────
langchain==0.2.1
langchain-community==0.2.1
langchain-openai==0.1.8        # OpenAI embeddings (optional)

# ── Document parsers ───────────────────────────────────────────────────────────
pypdf==4.2.0                   # PDF loader
docx2txt==0.8                  # DOCX loader
unstructured==0.14.4           # Markdown loader

# ── LLM clients ────────────────────────────────────────────────────────────────
openai==1.30.1                 # OpenAI API  (optional — only needed if using OpenAI)
requests==2.32.2               # Ollama HTTP calls

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # load .env file
pydantic==2.7.1                # request/response schemas (bundled with FastAPI)